{% extends "AdminBundle::base.html.twig" %}

{% block body %}
<main class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Liste des états de commande</h1>

            <table class="records_list table table-striped">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Type</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for entity in entities %}
                    <tr>
                        <td><a href="{{ path('admin_stateorder_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                        <td>{{ entity.type }}</td>
                        <td>
                        <ul class="no-list">
                            <li>
                                <a href="{{ path('admin_stateorder_show', { 'id': entity.id }) }}" class="btn btn-default btn-xs">voir</a>
                            </li>
                            <li>
                                <a href="{{ path('admin_stateorder_edit', { 'id': entity.id }) }}" class="btn btn-default btn-xs">éditer</a>
                            </li>
                        </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

                <ul class="no-list">
                <li>
                    <a href="{{ path('admin_stateorder_new') }}" class="btn btn-primary">
                        Créer une nouvelle entrée
                    </a>
                </li>
            </ul>
        </div><!-- end .col-md-12 -->
    </div><!-- end .row -->
</main>
{% endblock %}
