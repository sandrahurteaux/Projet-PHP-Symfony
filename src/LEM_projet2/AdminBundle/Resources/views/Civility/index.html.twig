{% extends "AdminBundle::base.html.twig" %}

{% block body %}
<main class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Liste des civilités</h1>

            <table class="records_list table table-striped">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nom</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for entity in entities %}
                    <tr>
                        <td><a href="{{ path('admin_civility_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                        <td>{{ entity.name }}</td>
                        <td>
                        <ul class="no-list">
                            <li>
                                <a href="{{ path('admin_civility_show', { 'id': entity.id }) }}" class="btn btn-default btn-xs">voir</a>
                            </li>
                            <li>
                                <a href="{{ path('admin_civility_edit', { 'id': entity.id }) }}" class="btn btn-default btn-xs">editer</a>
                            </li>
                        </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            <ul class="no-list">
                <li>
                    <a href="{{ path('admin_civility_new') }}" class="btn btn-primary">
                        Créer une nouvelle entrée
                    </a>
                </li>
            </ul>
        </div><!-- end .col-md-12 -->
    </div><!-- end .row -->
</main>
{% endblock %}
